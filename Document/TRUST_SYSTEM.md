# 모아(moa) 신뢰도 시스템

## 📌 개요

**핵심 컨셉**: "경험을 모아, 성장하다"

모아만의 독특한 신뢰도 시스템으로, 단순한 점수가 아닌 **성장과 경험의 시각화**를 통해 사용자의 활동을 표현합니다.

---

## 🌱 1. 성장 레벨 시스템 (Main Indicator)

### 레벨 구조: 씨앗 → 열매 맺는 나무

```
레벨 7: 🌳🍎 열매나무 (1000+ 포인트, 전설적 존재)
레벨 6: 🌲 큰 나무 (700-999 포인트, 모아의 기둥)
레벨 5: 🌳 나무 (500-699 포인트, 신뢰의 상징)
레벨 4: 🌿 작은 나무 (300-499 포인트, 성장 중)
레벨 3: 🪴 화분 (150-299 포인트, 뿌리 내림)
레벨 2: 🌱 새싹 (50-149 포인트, 첫 발걸음)
레벨 1: 🌰 씨앗 (0-49 포인트, 이제 시작)
```

### 성장 포인트 계산 방식

```javascript
성장 포인트 =
  참석한 모임 수 × 10 +
  주최한 모임 수 × 20 +
  받은 좋은 리뷰(4점 이상) 수 × 5 +
  챌린지 완료 수 × 15 +
  연속 출석 보너스
```

### 레벨업 메시지 (UX)

```
레벨 1→2: "🌱 새싹이 되셨어요! 모아에서의 첫걸음을 축하합니다."
레벨 2→3: "🪴 화분으로 성장했어요! 뿌리를 내리고 있네요."
레벨 3→4: "🌿 작은 나무가 되었어요! 꾸준히 성장 중이에요."
레벨 4→5: "🌳 나무로 성장했어요! 이제 모아의 신뢰받는 멤버입니다."
레벨 5→6: "🌲 큰 나무가 되었어요! 모아를 지탱하는 기둥이 되었습니다."
레벨 6→7: "🌳🍎 열매나무 달성! 모아의 전설이 되었습니다!"
```

---

## 🏅 2. 뱃지 시스템 (Trust Badges)

### A. 기본 뱃지 (참여 관련)

| 뱃지 | 이름 | 획득 조건 | 설명 |
|------|------|-----------|------|
| ✅ | 출석왕 | 참석률 95% 이상 | 약속을 잘 지키는 사람 |
| ⏰ | 시간지킴이 | 지각 0회, 10회 이상 참석 | 시간을 소중히 여기는 사람 |
| 💬 | 수다쟁이 | 채팅 메시지 100개 이상 | 활발한 소통을 즐기는 사람 |
| ❤️ | 인기쟁이 | 평균 리뷰 4.5점 이상 | 함께하면 즐거운 사람 |
| 🎯 | 완주자 | 챌린지형 모임 5개 완료 | 끝까지 해내는 사람 |
| 💝 | 친절왕 | 신규 멤버 도움 5회 | 따뜻한 마음을 가진 사람 |

### B. 호스트 뱃지 (주최자 관련)

| 뱃지 | 이름 | 획득 조건 | 설명 |
|------|------|-----------|------|
| 🎪 | 모임 메이커 | 모임 5개 이상 주최 | 모임을 만드는 사람 |
| 👑 | 슈퍼 호스트 | 평균 만족도 4.8점 이상 | 최고의 호스트 |
| 🔥 | 인기 모임 | 참여 신청 마감 10회 | 사람들이 좋아하는 모임 |
| 🌈 | 다양성 마스터 | 3개 이상 카테고리 주최 | 다채로운 경험 제공자 |
| 🎨 | 크리에이터 | 독창적인 모임 10개 주최 | 창의적인 기획자 |

### C. 특별 뱃지 (희귀)

| 뱃지 | 이름 | 획득 조건 | 설명 |
|------|------|-----------|------|
| 🦄 | 얼리버드 | 서비스 초기 100명 가입자 | 모아의 시작을 함께한 사람 |
| 🎉 | 파티피플 | 한 달 내 10개 모임 참여 | 모임을 사랑하는 사람 |
| 🌍 | 탐험가 | 5개 이상 지역 모임 참여 | 새로운 곳을 탐험하는 사람 |
| 📚 | 평론가 | 리뷰 50개 이상 작성 | 성실한 피드백 제공자 |
| 💌 | 추천왕 | 친구 초대 5명 이상 | 모아를 알리는 전도사 |
| 🏆 | 레전드 | 모든 뱃지 획득 | 모아의 완전체 |

### D. 계절 한정 뱃지

| 뱃지 | 이름 | 획득 조건 | 기간 |
|------|------|-----------|------|
| 🌸 | 봄나들이 | 3-5월 야외 모임 5회 | 봄 시즌 |
| ☀️ | 여름열정 | 6-8월 모임 10회 | 여름 시즌 |
| 🍂 | 가을감성 | 9-11월 문화 모임 5회 | 가을 시즌 |
| ⛄ | 겨울온기 | 12-2월 실내 모임 5회 | 겨울 시즌 |

---

## ✨ 3. 모아 포인트 시스템

### 포인트 획득

```
✨ 모임 참석: 10P
✨ 모임 주최: 30P
✨ 리뷰 작성: 5P
✨ 새 친구 초대 (가입): 20P
✨ 초대 친구 첫 모임 참여: 30P 보너스
✨ 연속 출석 (스트릭): +5P 보너스
✨ 챌린지 완료: 15P
✨ 월간 베스트 리뷰 선정: 50P
```

### 포인트 사용

```
🎫 유료 클래스 5% 할인 쿠폰: 300P
🎫 유료 클래스 10% 할인 쿠폰: 500P
🎯 프로필 부스팅 (24시간): 300P
☕ 스타벅스 아메리카노: 800P
🍰 디저트 카페 기프티콘: 1,000P
🏆 VIP 뱃지 (1개월): 2,000P
🎨 프로필 테마 (영구): 1,500P
```

### 포인트 만료

- 포인트는 획득일로부터 **1년간 유효**
- 만료 30일 전 알림 발송
- 사용 시 먼저 획득한 포인트부터 차감

---

## 🔥 4. 스트릭(Streak) 시스템

### 스트릭 정의

**연속으로 모임에 참여한 일수**를 측정합니다.

```
🔥 스트릭 3일: "활발한 시작!"
🔥🔥 스트릭 7일: "일주일 완주!"
🔥🔥🔥 스트릭 30일: "이번 달 완벽!"
🔥🔥🔥🔥 스트릭 90일: "모아 마스터!"
🔥🔥🔥🔥🔥 스트릭 365일: "1년 연속! 레전드!"
```

### 스트릭 보상

| 스트릭 | 보상 |
|--------|------|
| 7일 | +10P 보너스 |
| 30일 | +50P 보너스 + 🔥 배지 |
| 90일 | +150P 보너스 + 🔥🔥 배지 |
| 365일 | +500P 보너스 + 🔥🔥🔥 배지 + 특별 칭호 |

### 스트릭 유지 규칙

- 24시간 이내 다음 모임 참여 시 유지
- 주말 제외 옵션 (선택 가능)
- "스트릭 지키기" 아이템으로 1회 복구 가능 (200P)

---

## 📸 5. 순간 컬렉션 (Moment Collection)

### 자동 수집되는 순간들

**첫 경험 시리즈**:
```
📸 첫 모임 참여
🎉 첫 호스팅
💬 첫 채팅 참여
⭐ 첫 리뷰 작성
🤝 첫 친구 초대
```

**마일스톤 시리즈**:
```
🎊 10번째 모임
🎁 50번째 모임
💯 100번째 모임
🏆 500번째 모임
```

**관계 시리즈**:
```
🤝 같은 사람과 5번 모임
💕 같은 사람과 10번 모임
👥 10명의 새 친구 만남
🌟 50명의 새 친구 만남
```

**성과 시리즈**:
```
⭐ 5점 리뷰 받기
🌟 5점 리뷰 10회
✨ 베스트 리뷰 선정
🎯 챌린지 첫 완주
```

**희귀 순간**:
```
🌈 같은 날 3개 모임 참여
🎪 하루에 모임 2개 주최
🦄 레벨 7 도달
💎 모든 뱃지 수집
```

### 순간 표시 UI

```
📸 모인 순간들: 47개
최근 순간: "🎉 첫 호스팅" (2일 전)
희귀 순간: "💯 100번째 모임" ⭐
다음 순간: "🤝 같은 사람과 5번 모임" (3회/5회)
```

---

## 🌳 6. 관심사 나무 (Interest Forest)

### 카테고리별 성장 시각화

```
내 관심사 숲:

🎵 음악     🌳🌳🌳🌳 (28회) ← 가장 활발
🍳 요리     🌳🌳🌳   (15회)
📚 독서     🌳🌳     (8회)
⚽ 운동     🌳       (4회)
🎨 예술     🌱       (1회) ← 새로 시작
```

### 나무 단계

```
🌳🌳🌳🌳🌳 거목 (50회 이상) - 전문가 수준
🌳🌳🌳🌳 큰 나무 (30-49회) - 숙련자
🌳🌳🌳 나무 (15-29회) - 경험자
🌳🌳 작은 나무 (8-14회) - 초급자
🌳 묘목 (3-7회) - 입문자
🌱 새싹 (1-2회) - 첫 경험
```

### 다양성 보너스

**3개 카테고리 각 10회 이상**: 🌈 다양성 뱃지 + 50P
**5개 카테고리 각 5회 이상**: 🎨 탐험가 뱃지 + 100P

---

## 📊 7. 프로필 화면 구성

### 전체 레이아웃

```
┌──────────────────────────────────────┐
│  👤 김모아                            │
│  @kimoa  |  가입 3개월               │
├──────────────────────────────────────┤
│                                       │
│  🌳 레벨 5 - 나무                     │
│  성장 포인트: 523 / 700 (다음 레벨)   │
│  ▓▓▓▓▓▓▓░░░ 74%                      │
│                                       │
├──────────────────────────────────────┤
│  🏅 획득 뱃지 (7/15)                  │
│  ┌─────┬─────┬─────┬─────┬─────┐    │
│  │ ✅  │ ⏰  │ 💬  │ 🎯  │ 🎪  │    │
│  │출석왕│시간 │수다 │완주자│모임 │    │
│  │     │지킴 │쟁이 │     │메이커│    │
│  ├─────┼─────┼─────┴─────┴─────┘    │
│  │ ❤️  │ 🦄  │                      │
│  │인기 │얼리 │   + 더보기            │
│  │쟁이 │버드 │                      │
│  └─────┴─────┘                      │
│                                       │
├──────────────────────────────────────┤
│  🔥 현재 스트릭: 15일                 │
│  🔥🔥                                │
│  다음 보상까지 15일 남음              │
│                                       │
│  ✨ 모아 포인트: 420P                 │
│  이번 달 획득: +120P                  │
│                                       │
├──────────────────────────────────────┤
│  📸 모인 순간 (23개)                  │
│  최근: 🎉 10번째 모임 (1일 전)        │
│  희귀: 💯 100번째 모임 ⭐             │
│                                       │
├──────────────────────────────────────┤
│  🌳 관심사 숲                         │
│  🎵 음악    🌳🌳🌳🌳 (28회)          │
│  🍳 요리    🌳🌳🌳   (15회)          │
│  📚 독서    🌳🌳     (8회)           │
│  ⚽ 운동    🌳       (4회)           │
│  🎨 예술    🌱       (1회)           │
│                                       │
├──────────────────────────────────────┤
│  📊 활동 통계                         │
│  참석률: 95% (38/40)                  │
│  평균 리뷰: ⭐⭐⭐⭐⭐ 4.8            │
│  주최 모임: 5개                       │
│  총 참여: 38개                        │
└──────────────────────────────────────┘
```

---

## 🗄️ 8. 데이터베이스 설계

### 새로운 테이블

#### user_levels (사용자 레벨)
```sql
CREATE TABLE user_levels (
  id UUID PRIMARY KEY,
  user_id UUID UNIQUE REFERENCES users(id),
  level INTEGER DEFAULT 1,
  growth_points INTEGER DEFAULT 0,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
```

#### badges (뱃지 마스터)
```sql
CREATE TABLE badges (
  id UUID PRIMARY KEY,
  code VARCHAR(50) UNIQUE,
  name VARCHAR(100),
  description TEXT,
  icon VARCHAR(10),
  category VARCHAR(50), -- 'basic', 'host', 'special', 'seasonal'
  condition_type VARCHAR(50),
  condition_value INTEGER,
  is_active BOOLEAN DEFAULT true,
  created_at TIMESTAMP DEFAULT NOW()
);
```

#### user_badges (사용자 획득 뱃지)
```sql
CREATE TABLE user_badges (
  id UUID PRIMARY KEY,
  user_id UUID REFERENCES users(id),
  badge_id UUID REFERENCES badges(id),
  earned_at TIMESTAMP DEFAULT NOW(),
  UNIQUE(user_id, badge_id)
);
```

#### user_points (포인트 내역)
```sql
CREATE TABLE user_points (
  id UUID PRIMARY KEY,
  user_id UUID REFERENCES users(id),
  points INTEGER,
  balance INTEGER,
  type VARCHAR(50), -- 'earn', 'spend'
  source VARCHAR(100), -- 'gathering_attend', 'review', etc.
  description TEXT,
  expires_at TIMESTAMP,
  created_at TIMESTAMP DEFAULT NOW()
);
```

#### user_streaks (스트릭)
```sql
CREATE TABLE user_streaks (
  id UUID PRIMARY KEY,
  user_id UUID UNIQUE REFERENCES users(id),
  current_streak INTEGER DEFAULT 0,
  longest_streak INTEGER DEFAULT 0,
  last_activity_date DATE,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
```

#### moment_collections (순간 컬렉션)
```sql
CREATE TABLE moment_collections (
  id UUID PRIMARY KEY,
  user_id UUID REFERENCES users(id),
  moment_code VARCHAR(50),
  moment_name VARCHAR(100),
  moment_icon VARCHAR(10),
  is_rare BOOLEAN DEFAULT false,
  earned_at TIMESTAMP DEFAULT NOW(),
  UNIQUE(user_id, moment_code)
);
```

#### interest_forest (관심사 나무)
```sql
CREATE TABLE interest_forest (
  id UUID PRIMARY KEY,
  user_id UUID REFERENCES users(id),
  category_id UUID REFERENCES categories(id),
  participation_count INTEGER DEFAULT 0,
  tree_level INTEGER DEFAULT 1,
  updated_at TIMESTAMP DEFAULT NOW(),
  UNIQUE(user_id, category_id)
);
```

---

## 🎯 9. API 엔드포인트

### 레벨 & 성장

```
GET  /api/users/:id/level          # 레벨 정보 조회
POST /api/users/:id/level/points   # 포인트 추가 (내부 API)
```

### 뱃지

```
GET  /api/badges                    # 전체 뱃지 목록
GET  /api/users/:id/badges          # 사용자 획득 뱃지
POST /api/users/:id/badges/check   # 뱃지 획득 조건 체크 (내부)
```

### 포인트

```
GET  /api/users/:id/points          # 포인트 잔액 & 내역
POST /api/users/:id/points/earn     # 포인트 획득
POST /api/users/:id/points/spend    # 포인트 사용
GET  /api/points/rewards            # 사용 가능한 리워드 목록
```

### 스트릭

```
GET  /api/users/:id/streak          # 스트릭 정보
POST /api/users/:id/streak/update  # 스트릭 업데이트 (내부)
POST /api/users/:id/streak/restore # 스트릭 복구 (200P)
```

### 순간 컬렉션

```
GET  /api/users/:id/moments         # 수집한 순간 목록
POST /api/users/:id/moments/collect # 순간 수집 (내부)
```

### 관심사 나무

```
GET  /api/users/:id/forest          # 관심사 숲 조회
POST /api/users/:id/forest/grow    # 나무 성장 (내부)
```

---

## 🎨 10. UI/UX 가이드라인

### 컬러 시스템

```css
/* 레벨별 컬러 */
--level-1: #8B7355  /* 씨앗 - 갈색 */
--level-2: #90EE90  /* 새싹 - 연두색 */
--level-3: #32CD32  /* 화분 - 초록색 */
--level-4: #228B22  /* 작은 나무 - 진한 초록 */
--level-5: #006400  /* 나무 - 더 진한 초록 */
--level-6: #2F4F4F  /* 큰 나무 - 다크 그린 */
--level-7: #FFD700  /* 열매나무 - 골드 */

/* 뱃지 컬러 */
--badge-basic: #60A5FA    /* 파랑 */
--badge-host: #A78BFA     /* 보라 */
--badge-special: #F59E0B  /* 주황 */
--badge-seasonal: #EC4899 /* 핑크 */

/* 포인트 */
--point-color: #FBBF24    /* 골드 */

/* 스트릭 */
--streak-color: #EF4444   /* 빨강 (불꽃) */
```

### 애니메이션

```css
/* 레벨업 애니메이션 */
@keyframes levelup {
  0% { transform: scale(1); }
  50% { transform: scale(1.2) rotate(5deg); }
  100% { transform: scale(1) rotate(0deg); }
}

/* 뱃지 획득 애니메이션 */
@keyframes badge-earn {
  0% { transform: translateY(-20px); opacity: 0; }
  60% { transform: translateY(5px); opacity: 1; }
  100% { transform: translateY(0); }
}

/* 포인트 증가 애니메이션 */
@keyframes point-increase {
  0% { transform: translateY(0); opacity: 1; }
  100% { transform: translateY(-30px); opacity: 0; }
}
```

### 토스트 메시지

```javascript
// 레벨업
"🎉 축하합니다! 레벨 5 '나무'가 되었어요!"

// 뱃지 획득
"🏅 새로운 뱃지 획득! '출석왕' 뱃지를 받았어요!"

// 포인트 획득
"✨ +10P 획득! (잔액: 430P)"

// 스트릭
"🔥 7일 연속 참여! 스트릭 보너스 +10P"

// 순간 수집
"📸 새로운 순간! '100번째 모임' 순간을 모았어요!"
```

---

## 📈 11. 분석 지표 (Analytics)

### 추적할 KPI

**참여 지표**:
- 평균 레벨
- 레벨별 사용자 분포
- 월간 레벨업 수

**뱃지 지표**:
- 뱃지별 획득률
- 평균 보유 뱃지 수
- 가장 인기 있는 뱃지

**포인트 지표**:
- 월간 포인트 발행량
- 월간 포인트 사용량
- 리워드별 교환율

**스트릭 지표**:
- 평균 스트릭 일수
- 스트릭 30일 이상 사용자 비율
- 스트릭 복구 아이템 사용률

**관심사 지표**:
- 카테고리별 참여도
- 평균 관심사 개수
- 다양성 보너스 획득률

---

## 🚀 12. 구현 우선순위

### Phase 1: MVP (Week 9-10)
- [x] 성장 레벨 시스템
- [x] 기본 뱃지 6개 (출석왕, 시간지킴이, 수다쟁이, 인기쟁이, 완주자, 모임메이커)
- [x] 참석률 & 리뷰 평점 표시
- [x] 레벨 프로그레스 바

### Phase 2: 확장 (Month 1-2)
- [ ] 모아 포인트 시스템 전체
- [ ] 호스트 뱃지 5개
- [ ] 스트릭 시스템
- [ ] 포인트 리워드 스토어

### Phase 3: 게이미피케이션 (Month 3-4)
- [ ] 순간 컬렉션
- [ ] 관심사 나무
- [ ] 특별 뱃지 & 계절 뱃지
- [ ] 리더보드 (선택)

### Phase 4: 고도화 (Month 5+)
- [ ] AI 기반 뱃지 추천
- [ ] 커스텀 뱃지 (프리미엄)
- [ ] 뱃지 거래 마켓 (선택)

---

## 📝 참고 문서

- [PRD](./PRD.md)
- [DB Schema](./DB_SCHEMA.md)
- [API Specification](./API_SPECIFICATION.md)
- [Roadmap](./ROADMAP.md)

---

**문서 버전**: 1.0
**최종 수정일**: 2025-11-10
**작성자**: Product & Design Team
