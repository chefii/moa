# Prisma 디렉토리 구조

## 📁 디렉토리 설명

### `/prisma`
Prisma 스키마 및 데이터베이스 관리 파일들이 위치한 디렉토리입니다.

## 📄 파일 설명

### 스키마 파일
- **`schema.prisma`**: Prisma 스키마 정의 파일 (데이터베이스 모델, 관계, 인덱스 등)

### Seed 스크립트 (초기 데이터 투입)
- **`seed-all.ts`**: 전체 seed 스크립트 통합 실행
- **`seed-common-codes.ts`**: 공통 코드 초기 데이터
- **`seed-badges.ts`**: 배지 초기 데이터
- **`seed-categories.ts`**: 카테고리 초기 데이터
- **`seed-board-categories.ts`**: 게시판 카테고리 초기 데이터
- **`seed-board-menu.ts`**: 게시판 메뉴 초기 데이터
- **`seed-category-type.ts`**: 카테고리 타입 초기 데이터
- **`seed-menu.ts`**: 관리자 메뉴 초기 데이터
- **`seed-notifications.ts`**: 알림 초기 데이터
- **`seed-reports.ts`**: 신고 관련 초기 데이터
- **`seed-report-status.ts`**: 신고 상태 초기 데이터
- **`seed-settings.ts`**: 사이트 설정 초기 데이터
- **`seed-condition-types.ts`**: 조건 타입 초기 데이터

### 유틸리티 스크립트
- **`reset-database.ts`**: 데이터베이스 초기화 (전체 데이터 삭제 후 재시딩)

### `/archive`
이미 실행 완료된 일회성 마이그레이션 및 정리 스크립트들이 보관된 디렉토리입니다.
- 마이그레이션 스크립트 (`migrate-*.ts`)
- 정리 스크립트 (`cleanup-*.ts`, `clear-*.ts`)
- 업데이트 스크립트 (`update-*.ts`, `fix-*.ts`)
- 코멘트 추가 스크립트 (`add-*.ts`)
- 카테고리 구조 변경 스크립트 (`create-hierarchy.ts`, `flatten-categories.ts`, `restore-hierarchy.ts`)

## 🚀 사용 방법

### Seed 실행
```bash
# 전체 seed 실행
npm run prisma:seed-all

# 개별 seed 실행
npx ts-node prisma/seed-common-codes.ts
npx ts-node prisma/seed-categories.ts
```

### 데이터베이스 초기화
```bash
# ⚠️ 주의: 모든 데이터가 삭제됩니다!
npx ts-node prisma/reset-database.ts
```

### 마이그레이션
```bash
# 마이그레이션 생성 및 적용
npm run prisma:migrate

# 마이그레이션만 적용
npx prisma migrate deploy
```

### Prisma Client 생성
```bash
npm run prisma:generate
```

## 📝 참고사항

- **Seed 스크립트**: 개발 환경에서 초기 데이터를 투입할 때 사용
- **Archive 디렉토리**: 일회성 작업이 완료된 스크립트들이 보관되어 있으며, 필요시 참고용으로 사용
- **Schema 변경**: `schema.prisma` 파일 수정 후 반드시 마이그레이션 생성 및 적용 필요
